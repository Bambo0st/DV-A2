Open the current directory in VsCode.
Open python in VsCode.
You need to install python library,type the following in terminal
	>pip3 install cartopy
	>pip3 install matplotlib
Run the contour.py
	>python contour.py 
		 (OR)
	>python3 contour.py
The code only runs for one date Jan 1,2022.
If you want the the code to plot multiple dates, uncomment the date() function calls at the bottom.